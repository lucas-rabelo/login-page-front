import { tv } from "tailwind-variants"

export const containerForm = tv({
  base: "flex flex-col items-center justify-center pb-12 pt-[300px] md:py-5 px-8 h-auto md:h-screen w-full lg:w-1/2 bg-white"
});